# ซิงก์ข้ามเครื่อง — ข้อมูลอยู่ที่เดียว

> เพิ่มเมื่อ **8 ส.ค. 2569** · service worker `finflow-v11`

## ปัญหาเดิม

ข้อมูลทั้งหมดอยู่ใน `localStorage` ของเบราว์เซอร์เครื่องนั้นเท่านั้น
เปิดเว็บเดียวกันจากมือถือหรือคอมอีกเครื่อง จะเห็น **ข้อมูลตั้งต้นจาก `data/seed.json`**
(ค้างอยู่ที่ commit วันที่ 2 ส.ค.) ไม่ใช่ของที่แก้ล่าสุด — และต่างเครื่องต่างแก้ก็แยกกันไปคนละทาง

## แก้ยังไง

```
เบราว์เซอร์ (ทุกเครื่อง)          Cloudflare
┌──────────────────┐            ┌─────────────────────────┐
│ localStorage     │  ⇅ HTTPS   │ Worker: finflow-sync    │
│ finflow.v1       │ ─────────► │  └ Durable Object "ห้อง"│
│ (ยังเก็บครบเหมือนเดิม)│            │     SQLite: doc + hist  │
└──────────────────┘            └─────────────────────────┘
```

- **ในเครื่องยังเก็บครบเหมือนเดิม** คลาวด์เป็นแค่ตัวกลาง → เน็ตล่มหรือไม่เปิดซิงก์ ก็ใช้แอปได้ปกติ
- **หนึ่งรหัสซิงก์ = หนึ่งห้อง** ทุกเครื่องที่ใส่รหัสเดียวกันอ่าน-เขียนก้อนเดียวกัน
- ใช้ **Durable Object ไม่ใช่ KV** เพราะ KV อ่านค่าที่เพิ่งเขียนอาจได้ของเก่านานถึง 60 วินาที
  ("แก้บนมือถือแล้วเปิดคอมทันที" จะเห็นข้อมูลเก่า) — DO อ่านจากตัวเดียวเสมอ จึงตรงทันที

## เซิร์ฟเวอร์

`worker/src/index.js` → https://finflow-sync.patcharapol2298.workers.dev
(deploy ด้วย `npx wrangler deploy -c worker/wrangler.jsonc` — บัญชี Cloudflare ของผู้ใช้เอง)

| เส้นทาง | ทำอะไร |
|---|---|
| `GET /v1/ping` | เช็กว่าเซิร์ฟเวอร์ยังอยู่ (ไม่ต้องใช้รหัส) |
| `GET /v1/doc` | อ่านฉบับปัจจุบัน → `{rev, updatedAt, device, data}` (`rev:0` = ห้องว่าง) |
| `PUT /v1/doc` | เขียนทับ ต้องส่ง `{baseRev, device, data}` |
| `PUT /v1/doc?force=1` | เขียนทับโดยไม่สนใจ `baseRev` (ใช้ตอนผู้ใช้เลือก "ส่งของเครื่องนี้ทับ") |
| `GET /v1/history` | รายการฉบับย้อนหลัง (เก็บ 30 ฉบับ ไม่ส่งเนื้อข้อมูล) |
| `GET /v1/history/{rev}` | ขอเนื้อของฉบับย้อนหลังฉบับหนึ่ง |

**ความปลอดภัย**
- รหัสส่งทาง header `X-Sync-Code` ไม่ใช่ทาง URL (URL มีโอกาสไปโผล่ใน log ของตัวกลาง)
- ชื่อห้องคือ `SHA-256('finflow-sync:' + รหัส)` — ตัวรหัสไม่ถูกเก็บไว้ที่ไหนเลย
- CORS ยอมเฉพาะ `kp-innovation.github.io` กับ localhost — เว็บอื่นเรียกผ่านเบราว์เซอร์ผู้ใช้ไม่ได้
- **ใครมีรหัสก็เปิดดูข้อมูลได้** จึงสุ่มให้ 20 ตัว (ตัด `0 O 1 l I` ออกเพราะต้องพิมพ์ข้ามเครื่อง)

## กติกาตอนข้อมูลชนกัน

`rev` เป็นเลขฉบับที่เพิ่มทีละ 1 ทุกครั้งที่เขียน เครื่องที่จะเขียนต้องบอกว่าตัวเองถือ `rev` อะไรอยู่
ถ้าไม่ตรงกับของจริง = มีเครื่องอื่นแก้ไปก่อน → เซิร์ฟเวอร์ตอบ **409 พร้อมข้อมูลจริง**

แล้วแอปจะ**ถามผู้ใช้เสมอ ไม่ทับให้เอง** (แถบเหลืองบนสุดของหน้า):

| ปุ่ม | ผลลัพธ์ |
|---|---|
| ⬇ ใช้ของบนคลาวด์ | เอาของคลาวด์มาทับในเครื่อง |
| ⬆ ส่งของเครื่องนี้ทับ | ส่งด้วย `?force=1` ทับของบนคลาวด์ |
| 💾 สำรองทั้งสองฝั่งก่อน | โหลดไฟล์ JSON ทั้งสองฝั่งเก็บไว้ก่อนตัดสินใจ |

เหตุผล: ข้อมูลการเงินหายแล้วหาคืนไม่ได้ · เผื่อพลาดจริงๆ เซิร์ฟเวอร์ยังเก็บ 30 ฉบับย้อนหลังไว้ใน `hist`

## จังหวะการซิงก์ (`js/sync.js`)

| เมื่อไหร่ | ทำอะไร |
|---|---|
| แก้อะไรก็ตามในแอป | รอ 1.2 วิ (รวบหลายการแก้เป็นครั้งเดียว) แล้วส่งขึ้น |
| เปิดแอป | ดึงของล่าสุดลงมา |
| กลับมาที่หน้าจอ (เกิน 15 วิ) | ดึงของล่าสุดลงมา |
| ทุก 1 นาทีตอนเปิดหน้าอยู่ | ดึงของล่าสุดลงมา |
| **หน้าจอถูกซ่อน / ปิดแท็บ** | ถ้ายังมีของค้าง **ส่งทันที ไม่รอ** |
| ส่งไม่สำเร็จ | เก็บไว้ในเครื่อง แล้วลองใหม่เองใน 10 วิ (เซิร์ฟเวอร์พัง = 30 วิ) |
| เน็ตกลับมา (`online`) | ส่งของค้าง / ดึงของใหม่ |

### สามเรื่องที่พังตอนทำ แล้วแก้ไปแล้ว (อย่าทำซ้ำ)

**1. ส่งกลับไปกลับมาไม่รู้จบ**
`timeline.js` เรียก `Store.save()` **ทุกครั้งที่วาดหน้า** (ไว้เก็บเดือนที่เพิ่งสร้าง)
พอมีซิงก์ กลายเป็น "มีการแก้ไข" ปลอมๆ → เครื่อง ก. ดึงของเครื่อง ข. มาแล้ววาดหน้า → ส่งกลับขึ้นไป
→ เครื่อง ข. ดึงแล้วส่งกลับ วนไปเรื่อยๆ
แก้สองชั้น: (ก) `timeline.js` บันทึกเฉพาะตอนสร้างเดือนใหม่จริง
(ข) `bump()` เทียบข้อมูลกับก้อนล่าสุดที่ตรงกับคลาวด์ (`sent`) ก่อน — เหมือนเดิมก็ไม่ส่ง

**2. วาดหน้าซ้อนกันจน stack ล้น**
ตัวฟังสถานะซิงก์เรียก `App.render()` ทันทีที่สถานะเปลี่ยน แต่การวาดหน้าเองก็ทำให้สถานะเปลี่ยนได้อีก
ตอนนี้เทียบกับ **ของจริงบนจอ** (`document.querySelector('.sync-clash, .sync-err')`) ว่าตรงกับที่ควรเป็นไหม
วาดเสร็จเงื่อนไขจะเป็นเท็จเอง จึงวนซ้ำไม่ได้ · และเลื่อนวาดด้วย `setTimeout(…, 0)` เสมอ
· **ห้ามวาดทับตอนผู้ใช้กำลังพิมพ์อยู่** (เคอร์เซอร์เด้งหลุด — บทเรียนเดียวกับโน้ตด่วน)

**3. ส่งไม่สำเร็จแล้วค้างตลอด**
เดิมส่งพลาดแล้วจบเลย ไม่มีใครลองใหม่ ต้องรอผู้ใช้แก้อะไรสักอย่างถึงจะได้ส่ง
ตอนนี้มี `retryLater()` + ธง `pushing` กันส่งซ้อนสองสาย (`baseRev` จะเพี้ยนจนชนกันเอง)

> เกร็ด: ตอนทดสอบเจออาการ "ค้าง" ที่ไม่ใช่บั๊ก — Chrome **หยุดจับเวลาในแท็บที่ไม่ได้อยู่หน้าจอ**
> ตัวจับเวลา 1.2 วิเลยไม่ทำงาน · นั่นคือเหตุผลที่ต้องมีข้อ "หน้าจอถูกซ่อน = ส่งทันที"

## วิธีใช้

**เครื่องแรก** — เมนู `⋯` (มือถือ: เพิ่มเติม → ข้อมูลของฉัน) → **เปิดใช้ (สร้างรหัสใหม่)**
ข้อมูลที่มีอยู่จะถูกส่งขึ้นเป็นฉบับแรก

**เครื่องต่อไป** — เลือกอย่างใดอย่างหนึ่ง
- กด **🔗 คัดลอกลิงก์เปิดบนมือถือ** แล้วส่งลิงก์ไปเปิดบนเครื่องนั้น (ลิงก์มี `#sync=รหัส` ตั้งค่าให้เอง)
- หรือกด **ใส่รหัสที่มีอยู่** แล้วพิมพ์รหัสเอง

เครื่องที่สองจะถามก่อนเสมอว่าจะใช้ของฝั่งไหน (คลาวด์ / ในเครื่อง) — ปกติเลือก **ใช้ของบนคลาวด์**

**ล้างข้อมูลเริ่มใหม่ทั้งที่เปิดซิงก์อยู่** → ของบนคลาวด์และเครื่องอื่นถูกล้างตามไปด้วย (มีเตือนในกล่องยืนยัน)
ทำได้เพราะตั้งธง `finflow.sync.reset` ไว้ ไม่งั้นรีโหลดเสร็จมันจะดึงของเก่ากลับมาทันที

## ทดสอบอะไรไปแล้ว

รันที่ `http://127.0.0.1:8788` ยิงเข้า Worker ตัวจริง

- ✅ เปิดซิงก์เครื่องแรก → 19 เดือนขึ้นคลาวด์เป็น `rev 1`
- ✅ แก้ยอด → ส่งขึ้นเองใน ~1.2 วิ · `rev` เพิ่ม · ค่าบนเซิร์ฟเวอร์ตรง
- ✅ จำลองมือถือเขียนแซง → เครื่องนี้ดึงลงมาใช้เงียบๆ ถูกต้อง (ทั้ง `localStorage` และบนจอ)
- ✅ สองฝั่งแก้ซ้อนกัน → ได้ 409 → แถบเลือกฝั่งโผล่ · กด **ใช้ของบนคลาวด์** และ **ส่งของเครื่องนี้ทับ** ได้ผลถูกทั้งคู่
- ✅ เครื่องใหม่ (ล้าง `localStorage`) ใส่รหัสเดิม → ถามก่อน แล้วดึงข้อมูลจริงมาครบ 19 เดือน
- ✅ เน็ตหลุดตอนกำลังแก้ → ข้อมูลอยู่ในเครื่องครบ + แถบเตือน · เน็ตกลับมา **ลองใหม่เองสำเร็จ** โดยไม่ต้องแตะอะไร
- ✅ วาดครบทุกหน้า 3 รอบ + ดึงข้อมูล 3 รอบ → `rev` นิ่งที่เดิม (ไม่มีการส่งกลับไปกลับมา)
- ✅ ปิดซิงก์แล้วแอปยังใช้ได้ปกติทุกอย่าง
- ✅ ภาษาไทยไป-กลับผ่านเซิร์ฟเวอร์ไม่เพี้ยน · คนละรหัส = คนละห้อง แยกกันสนิท · รหัสสั้นกว่า 12 ตัวถูกปฏิเสธ

## ยังไม่ได้ทำ

- [ ] รวมข้อมูลสองฝั่งแบบทีละรายการ (ตอนนี้เลือกได้แค่ทั้งก้อน)
- [ ] หน้าเรียกฉบับย้อนหลังจากในแอป — เซิร์ฟเวอร์เก็บ `hist` ไว้ให้แล้ว แต่ยังไม่มีปุ่มเรียกดู
- [ ] เข้ารหัสข้อมูลก่อนส่ง (ตอนนี้ Cloudflare อ่านเนื้อข้อมูลได้ถ้าอยากอ่าน)
- [ ] แจ้งเตือนแบบเรียลไทม์เมื่ออีกเครื่องแก้ (ตอนนี้รอรอบดึงทุก 1 นาที)
