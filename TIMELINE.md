# หน้า Timeline (เส้นเวลา) — บันทึกการพัฒนา

> อัปเดตล่าสุด **4 ส.ค. 2569** · ขึ้นเว็บแล้วที่ https://kp-innovation.github.io/SummaryMoney/
> service worker เวอร์ชัน `finflow-v9`

## แก้หลังใช้จริงรอบแรก (สำคัญ — อย่าทำซ้ำ)

**1. แต่ละเดือนต้องเลื่อนแนวนอนของตัวเอง**
เดิมทำเป็นแถบเลื่อนอันเดียวคุมทั้ง 12 เดือน เพื่อให้วันที่ตรงกันทุกแถวเวลาเทียบข้ามเดือน
ผลคือเลื่อนดูปลายเดือน ส.ค. ทีเดียว **เดือนอื่นเลื่อนตามหมด** ทั้งที่ไม่ได้ตั้งใจแตะมัน — งงมาก
ตอนนี้ `.tl-mscroll` อยู่ในแต่ละ `.tl-month` และจำตำแหน่งแยกกันใน `App.state.tlScrollX[mid]`
มีเงาขอบขวา (`.can-scroll` / `.at-end`) บอกว่ายังมีวันต่ออีกไหม

**2. แถบเครื่องมือบนมือถือห้ามตัดบรรทัด**
เดิม `flex-wrap: wrap` ทำให้ปุ่มเด้งเป็น 3–4 แถวไม่คงที่ ตำแหน่งปุ่มขยับทุกครั้งที่สถานะเปลี่ยน
ตอนนี้จอแคบ: ปุ่มเหลือแค่ไอคอน (`.btn-t` ถูกซ่อน) · `แถว`/`ช่วงจ่าย`/`กระชับ` ยุบเข้าปุ่ม **⚙ มุมมอง**
· หัวเดือนใช้ชื่อย่อและซ่อนตัวเลขส่วนต่าง เพื่อให้อยู่บรรทัดเดียว

**3. วัดขนาดต้องทำหลังต่อ DOM แล้ว**
`requestAnimationFrame` ใน `monthLine()` ไม่ทำงานตามคาดเพราะตอนนั้น node ยังไม่ถูก append
ย้ายไปวัดใน `board()` หลังวางทุกเดือนลงหน้าแล้ว

**4. ช่องพิมพ์บนมือถือต้อง 16px เป๊ะ**
iOS Safari ซูมจอเข้าเองทุกครั้งที่แตะช่องพิมพ์ที่ตัวอักษรเล็กกว่า 16px แล้วทั้งหน้าเพี้ยน
บังคับ 16px ให้ทุก input/textarea/select ในเบรกพอยต์มือถือแล้ว

---

## โน้ตด่วน (Quick Note)

แถบจดอิสระบนสุดของ **ทุกหน้า** — อยู่ใน `app.js` (`quickNote()`) ไม่ใช่ `timeline.js`
เพราะมันไม่ผูกกับเดือนหรือรายการไหน (ต่างจากปุ่ม ✎ ที่เป็นโน้ตของรายการนั้นๆ)

- สองสถานะ: **ย่อ** เป็นแถบบาง โชว์บรรทัดแรก ↔ **กาง** เป็นช่องพิมพ์ที่ยืดตามเนื้อหา (สูงสุด 45% ของจอ)
- บันทึกเองอัตโนมัติหลังหยุดพิมพ์ 0.4 วิ · เก็บใน `D.quickNote = { text, open, hidden, updated }`
- ปุ่ม **ซ่อนแถบนี้** = หายไปเลย · เปิดคืนที่เมนู **⋯ ข้อมูลของฉัน**
- ⚠️ **ห้ามเรียก `render()` ตอนกำลังพิมพ์** — DOM ถูกสร้างใหม่ทั้งหน้า เคอร์เซอร์จะเด้งหลุดกลางประโยค
  จึงใช้ `Store.save()` เฉยๆ ระหว่างพิมพ์ · และตอนกดปิด/ซ่อนต้อง `flush()` ก่อน
  ไม่งั้นตัวอักษรใน 0.4 วิสุดท้ายหาย

---

หน้านี้เป็น **หน้าแรกของแอป** แทนหน้า "ภาพรวม" เดิม
เจตนา: เปิดแอปมาต้องตอบได้ทันทีว่า *วันนี้ต้องจ่ายอะไรก่อน และเงินจะพอไหม*

---

## แนวคิดของหน้านี้

หนึ่งเดือน = เส้นแนวนอนหนึ่งเส้น (วันที่ 1 → สิ้นเดือน) · หนึ่งจอ = 12 เส้นนับจากเดือนปัจจุบัน
รายการเงินชี้เป็น **ลูกศรออกจากเส้น** ตามวันที่จริง

```
   ▲ เงินเดือน            ▲23,000
   ▲ เงินพิเศษ                    ▲21,500
   ▲ จ่ายออมเงิน                       ▲5,000
───┼────┼────┼────┼────┼────┼────┼────┼───  ← เส้นเวลา 1..31
   ▼ ค่าที่อยู่                         ▼7,000
   ▼ น้ำ-ไฟ-เน็ต     ▼1,200
   ▼ บัตร / หนี้              ▼2,400
```

- **เหนือเส้น** = เงินที่ยังเป็นของเรา (รายรับ · เงินออม · เงินฉุกเฉิน · ค่าผ่อน)
- **ใต้เส้น** = จ่ายแล้วหมดไป (ค่าน้ำ ค่าไฟ ค่าหอ ค่ารักษา ค่ากิน)
- แกน Y = "แถว (lane)" ที่ **เพิ่ม/ลบ/เปลี่ยนสี/สลับฝั่งเองได้** อนาคตมีค่าผ่อนรถก็เพิ่มแถวใหม่

**ข้อมูลใช้ชุดเดียวกับทุกหน้า** (`months[].incomes / oneTimes / expenses`)
แก้ที่เส้นเวลา → ปฏิทิน รายเดือน กราฟ ขยับตามทันที ไม่มีข้อมูลซ้อนสองชุด

---

## ทำอะไรไปแล้วบ้าง (เช็กลิสต์ตามที่สั่ง)

| สิ่งที่ขอ | สถานะ | อยู่ตรงไหน |
|---|---|---|
| เส้นเวลารายเดือน 1–31 · 12 เส้นต่อปี | ✅ | `monthLine()` |
| ลูกศรชี้ออกจากเส้นตามวันที่ | ✅ | `.tl-mk` + `.tl-head` |
| แบ่งบน (เงินดี) / ล่าง (จ่ายหมดไป) | ✅ | `lane.side` = `up` / `down` |
| แกน Y บอกประเภท · รายรับอยู่บนสุด สีเขียว | ✅ | `DEFAULT_LANES` ใน `store.js` |
| เพิ่มประเภทเอง (เช่น ค่าผ่อนรถ) | ✅ | ปุ่ม **แถว** → `laneSheet()` / `laneEdit()` |
| ปุ่ม Gen จำลองรายการซ้ำถึงสิ้นปี | ✅ | ปุ่ม **⚡ จำลอง** → `genSheet()` |
| เลือกได้ว่าจำลองตั้งแต่เดือนไหน ถึงเดือนไหน | ✅ | ดรอปดาวน์ เริ่ม/ถึง (ตั้งต้น = เดือนนี้ → ธ.ค.) |
| จำลองเฉพาะตั้งแต่วันนี้เป็นต้นไป | ✅ | ข้ามเดือนอดีต + ข้ามวันที่เลยไปแล้วในเดือนนี้ |
| ห้ามทับตัวเลขที่กรอกเองแล้ว | ✅ | ธง `it.locked` + `it.paid` |
| กลับมาแก้แต่ละจุดได้ (จ่ายเท่าไหร่ / รับเท่าไหร่) | ✅ | แตะลูกศร → `itemSheet()` |
| สรุปท้ายเส้นว่าเดือนนี้เหลือ/ติดลบ | ✅ | ป้ายท้ายเส้น + ชิปที่หัวเดือน |
| จุดสี แดง/ส้ม/เหลือง ตามลำดับที่ต้องจ่าย | ✅ | `queue()` — ใช้ทั้งหน้าเส้นเวลาและหน้ารายเดือน |
| ติ๊กจ่ายแล้ว → ขีดฆ่า + สีเลื่อนไปคิวถัดไป | ✅ | ปุ่ม ✓ ในแผงรายการ |
| Toggle ไฮไลต์ช่วงที่จ่ายได้โดยไม่เสียเครดิต | ✅ | ปุ่ม **ช่วงจ่าย** → `bandSheet()` · เลือกทีละรายการได้ |
| เลือกแสดง/ซ่อนแถวเพื่อประหยัดพื้นที่ | ✅ | ปุ่ม **แถว** → ไอคอน 👁 |
| เลื่อนช่วงวันซ้าย-ขวาเองได้ | ✅ | แถบสองหัวจับใต้แถบเครื่องมือ (ซูมเข้าไปดูครึ่งเดือน) |
| ปุ่มล็อกกันเผลอนิ้วไปโดน | ✅ | ปุ่ม **🔒 ล็อก / 🔓 แก้ไขอยู่** |
| ลากนิ้วกำหนดวันบนเส้นเวลา | ✅ | แถบลากในแผงเพิ่ม/แก้ + ลากตัวลูกศรตอนปลดล็อก |
| เพิ่มรายการในเดือนนั้นได้เลย (เข้า/ออก) | ✅ | ปุ่ม **+ เพิ่ม** ที่หัวเดือน |
| เงินเดือนวันเข้าไม่แน่นอน — default วันที่ 1 | ✅ | `addSheet()` ตั้งต้นวันที่ 1 แล้วมาแก้วัน+ยอดจริงทีหลัง |
| กรอกยอดเงินจริงในบัญชีสิ้นเดือน | ✅ | แตะชิปสรุปที่หัวเดือน → `balanceSheet()` |
| นับเงินฉุกเฉินกลับเข้าเงินสุทธิ (แถวอื่นห้ามนับ) | ✅ | `lane.emergency = true` → `chain()` |
| ทำให้ฟิตจอมือถือ / iPad | ✅ | ดูหัวข้อ "เรื่องจอ" ข้างล่าง |
| ชื่อแถวตรึงไว้ตอนเลื่อนดูสิ้นเดือน | ✅ | `.tl-labels { position: sticky; left: 0 }` |
| ทำให้เดือนเตี้ยที่สุดเพื่อเห็นหลายเดือน | ✅ | วาดเฉพาะแถวที่มีของ + โหมด **⇕ กระชับ** |

---

## ไฟล์ที่เพิ่ม / แก้

| ไฟล์ | ทำอะไร |
|---|---|
| **`js/timeline.js`** *(ใหม่ ~780 บรรทัด)* | ทั้งหน้า: การวาดเส้น · คิวจ่าย · ทุกแผงลอย · การจำลอง |
| `js/store.js` | เพิ่ม `lanes` · `guessLane()` · `laneOf()` · `month.balance` · `emergencyStart` · `cashNow` · ฟิลด์ `lane/grace/locked` ของทุกรายการ |
| `js/app.js` | หน้าตั้งต้น = `timeline` · `TL.bust()` ทุกครั้งที่วาด · เมนู **เพิ่มเติม** บนมือถือ · ช่องเงินฉุกเฉินตั้งต้น |
| `js/screens.js` | ลงทะเบียนหน้า `timeline` · จุดสีคิวจ่ายในหน้ารายเดือน · แสดง "ยอดจริง" คู่กับ "ตามแผน" |
| `css/app.css` | สไตล์ทั้งหมดของไทม์ไลน์ (~260 บรรทัด) · `scrollbar-gutter: stable` · ปุ่ม nav มือถือ |
| `index.html` | เมนู **เส้นเวลา** เป็นอันแรก · ปุ่ม **เพิ่มเติม** · `<script src="js/timeline.js">` |
| `sw.js` | `VERSION → finflow-v8` + เพิ่ม `timeline.js` ในลิสต์ |

---

## โครงข้อมูลที่เพิ่มเข้ามา

```jsonc
{
  "lanes": [                       // แกน Y — ผู้ใช้จัดการเองได้ทั้งหมด
    { "id":"salary", "name":"เงินเดือน", "side":"up", "color":"#34d399" },
    { "id":"emergency", "name":"เงินเก็บฉุกเฉิน", "side":"up",
      "color":"#818cf8", "emergency": true }   // ← แถวเดียวที่นับกลับเป็นเงินสุทธิ
  ],
  "emergencyStart": 0,             // เงินฉุกเฉินที่มีก่อนเดือนแรก
  "cashNow": null,                 // เงินในบัญชีตอนนี้ (ใช้คิดคิวจ่ายว่าพอไหม)

  "months": [{
    "balance": null,               // ยอดเงินจริงในบัญชีสิ้นเดือน — หมุดความจริง
    "expenses": [{
      "lane": "utility",           // อยู่แถวไหน
      "grace": 5,                  // จ่ายช้าได้อีกกี่วันโดยไม่เสียเครดิต
      "locked": true,              // ผู้ใช้กรอกเลขเอง → ปุ่มจำลองห้ามทับ
      "paid": true, "paidOn": "2569-08-14"
    }]
  }]
}
```

ของเก่าที่ไม่มีฟิลด์พวกนี้ **ไม่ต้องทำอะไร** — `normalize()` ใน `store.js` เดาแถวให้จากชื่อรายการ
(`guessLane()`: เจอ "ค่าไฟ/3BB" → น้ำ-ไฟ-เน็ต · "ผ่อน/กยศ" → จ่ายค่าผ่อน · "หอ/ห้อง/เช่า" → ค่าที่อยู่ …)
เดาผิดได้ ไม่พัง — เปิดแผงรายการแล้วเปลี่ยนแถวเองได้ทันที

---

## กติกาเงินสุทธิ (จุดที่แก้ "จุดบอด" ตามที่คุยกัน)

เอารายรับลบรายจ่ายตรงๆ ไม่มีทางตรง เพราะมีรายจ่ายจุกจิกที่ไม่ได้ลงเส้นเวลา
เลยเปลี่ยนมาใช้ **ยอดเงินจริงในบัญชีเป็นหมุด** แทน

```
เงินสุทธิสิ้นเดือน = ยอดเงินจริงในบัญชี + เงินเก็บฉุกเฉินสะสม
```

- **เงินฉุกเฉินเท่านั้น** ที่ถูกนับกลับ (แค่ย้ายบัญชี ยังเป็นเงินเรา)
- เงินออม / ค่าผ่อน ถึงจะอยู่เหนือเส้น **ก็ไม่นับ** เพราะดึงออกมาใช้ไม่ได้
- เดือนที่ยังไม่ได้กรอกยอดจริง → ต่อลูกโซ่จากเดือนล่าสุดที่กรอกไว้ แล้วบวก-ลบตามแผน ติดป้าย **"คาด"**
- เดือนอนาคตนับเงินฉุกเฉินตามแผน · เดือนที่ผ่านมาแล้วนับเฉพาะที่ติ๊กว่าโอนจริง

> ⚠️ ตัวเลขนี้ **คนละตัว** กับ "เหลือสุทธิตามแผน" ของ `engine.js` ที่หน้าอื่นใช้อยู่
> หน้ารายเดือนจึงโชว์ทั้งสองบรรทัดและติดป้ายกำกับไว้ ไม่ให้สับสนว่าอันไหนเชื่อได้

---

## คิวจ่าย แดง / ส้ม / เหลือง

`queue()` สแกน 3 เดือนข้างหน้า จัดรายจ่ายที่ยังไม่ติ๊กเป็นกลุ่มตาม **วันครบกำหนด** แล้วไล่สี

| สี | ความหมาย |
|---|---|
| 🔴 เข้ม `over` | เลยกำหนดแล้วแต่ยังไม่ติ๊ก |
| 🔴 `red` | กลุ่มที่ใกล้ที่สุด — ต้องหาเงินก้อนนี้ก่อน |
| 🟠 `orange` | กลุ่มถัดไป |
| 🟡 `yellow` | กลุ่มถัดไปอีก |

พอติ๊กว่าจ่ายแล้ว รายการหลุดจากคิว → สีเลื่อนไปกลุ่มถัดไปเอง
การ์ดบนสุดยังบอกด้วยว่าจ่ายกลุ่มนั้นแล้ว **เหลือ / ขาด** เท่าไหร่ (ต้องกรอก "เงินในบัญชีตอนนี้" ก่อน)
จุดสีชุดเดียวกันนี้ไปโผล่ในหน้า **รายเดือน** ด้วย

---

## เรื่องจอ (มือถือ / iPad)

- ความกว้างวัน (`dayW`) คำนวณจากที่ว่างจริง: **จอใหญ่บีบให้เห็นครบเดือนในตาเดียว · มือถือขั้นต่ำ 26px/วัน แล้วเลื่อนเอา**
- ชื่อแถวแกน Y `position: sticky; left: 0` — เลื่อนไปสิ้นเดือนแล้วยังรู้ว่าลูกศรไหนคือค่าอะไร
- หัวเดือน (พร้อมตัวเลขสุทธิ) sticky เหมือนกัน
- การ์ดเส้นเวลากินเต็มความกว้างจอบนมือถือ (ลบ margin ออกทั้งซ้าย-ขวา)
- `overscroll-behavior-x: contain` — ปัดแนวนอนแล้วเบราว์เซอร์ไม่ย้อนหน้า
- แถบล่างมือถือใส่ได้ 5 ปุ่ม (เส้นเวลา · ภาพรวม · ปฏิทิน · รายเดือน · เพิ่มเติม) ที่เหลือยุบเข้าเมนู **เพิ่มเติม**
- ป้ายตัวเลขบนลูกศรจะโผล่เฉพาะเมื่อ **ไม่ชนกับป้ายก่อนหน้าในแถวเดียวกัน** (คิดชนกันตอนวาด) แคบมากก็เหลือแค่หัวลูกศร

---

## จุดที่ต้องระวังเวลาแก้ต่อ

1. **ห้ามเปลี่ยน `.tl-labels` เป็น `position: absolute`** — sticky ใช้กับ absolute ไม่ได้
   ชื่อแถวจะหลุดจอตอนเลื่อนไปดูสิ้นเดือน
2. **ต้องวางการ์ดลงหน้าก่อนแล้วค่อยวัดความกว้าง** (`scroll.clientWidth`)
   เคยพลาดใช้ `host.clientWidth` ซึ่ง**รวม padding ของ `.main` เข้ามาด้วย** เลยคิดว่ามีที่มากกว่าจริง 52px
   แล้ววาดเส้นล้นจนต้องเลื่อนทั้งที่ควรพอดีจอ
3. **`.main` ต้องมี `scrollbar-gutter: stable`** ไม่งั้นความกว้างเปลี่ยนตอนแถบเลื่อนโผล่ แล้วเส้นล้นนิดนึงทุกครั้ง
4. **`TL.bust()` ต้องถูกเรียกทุกครั้งที่ `App.render()`** — คิวจ่ายถูกแคชไว้ต่อการวาดหนึ่งครั้ง
   (ไม่งั้นติ๊กจ่ายแล้วสียังไม่ขยับ)
5. **ปุ่มจำลองห้ามแตะ `locked` / `paid`** — เป็นสัญญาหลักของหน้านี้ กดจำลองทีเดียวแล้วตัวเลขที่บันทึกไว้หายคือพัง
6. **ย้ายแถวข้ามฝั่งบน↔ล่าง ต้องย้ายตะกร้าจริงด้วย** (`moveBasket()`)
   ไม่งั้น `engine.js` ยังคิดเป็นรายจ่ายอยู่ทั้งที่บนเส้นขึ้นไปอยู่ฝั่งรายรับแล้ว
7. **ขึ้น `VERSION` ใน `sw.js` ทุกครั้งที่แก้ไฟล์ในลิสต์** (ตอนนี้ `finflow-v8`)

---

## ยังไม่ได้ทำ / ทำต่อได้

- [ ] **ยังไม่ push ขึ้น GitHub Pages** — โค้ดอยู่ในเครื่องอย่างเดียว
- [ ] แถวเงินฉุกเฉินยัง**ไม่มีของจริงในข้อมูลตั้งต้น** — `guessLane()` จะจับก็ต่อเมื่อชื่อรายการมีคำว่า
      "ฉุกเฉิน" หรือ "สำรอง" · ตอนนี้เงินออมทั้งหมดตกไปแถว "จ่ายออมเงิน" ซึ่ง**ไม่ถูกนับเป็นเงินสุทธิ**
      → ถ้าอยากให้นับ ต้องเข้าไปเปลี่ยนแถวของรายการนั้นเป็น "เงินเก็บฉุกเฉิน" เอง
- [ ] รายการที่ยังไม่ระบุวัน (จากข้อมูล Excel เดิม ~9 รายการ/เดือน) วางบนเส้นไม่ได้
      ตอนนี้โชว์เป็นชิป **⚠ ยังไม่ระบุวัน N** ที่หัวเดือน แตะแล้วใส่วันได้ทีละอัน — ยังไม่มีโหมดใส่รวดเดียว
- [ ] ลากลูกศรตอนปลดล็อก **ย้ายทั้งกลุ่ม** ถ้าวันนั้นแถวนั้นมีหลายรายการ (ตั้งใจให้เป็นแบบนั้น แต่ยังไม่มีทางแยกลากทีละอัน)
- [ ] ยังไม่ผูกกับรอบบัตรเครดิตอัตโนมัติ — วันครบกำหนดของบัตรยังต้องใส่เอง
      (ข้อมูลบัตรมี `statementDay` / `dueDay` อยู่แล้วใน `cards[]` ดึงมาเติมให้อัตโนมัติได้)
- [ ] โหมดกระชับรวมทุกหมวดเป็นแถวเดียว ทำให้ป้ายตัวเลขซ้อนกันมากขึ้น — ถ้าใช้บ่อยอาจต้องทำ 2–3 แถวแบบสลับชั้นแทน

---

## ทดสอบอะไรไปแล้ว

รันที่ `http://127.0.0.1:8901` แล้วลองจริงบนเบราว์เซอร์:

- ✅ วาด 12 เดือน 93 ลูกศร ไม่มี error ใน console
- ✅ กว้างพอดีจอเป๊ะ (`scrollWidth === clientWidth`) บนจอเดสก์ท็อป
- ✅ มือถือ 390px — แถบล่าง 5 ปุ่ม · ชื่อแถวตรึงซ้าย · เลื่อนแนวนอนได้
- ✅ iPad 768px — ใช้เลย์เอาต์แถบล่าง ได้ความกว้างเต็มจอ
- ✅ แตะลูกศร → แผงแก้ → พิมพ์ `7000+200` → กด ✓ จ่ายแล้ว → ลูกศรถูกขีดฆ่า ยอดเดือนขยับ −200
- ✅ กด ⚡ จำลอง ส.ค.→ธ.ค. → สร้างรายการที่ขาดครบ · **ไม่แตะ** ค่าห้องที่ติ๊กจ่ายไปแล้ว
- ✅ กรอกยอดจริง 12,000 → เดือนนั้นเลิกเป็น "คาด" · เดือนถัดไปต่อลูกโซ่จากเลขจริง
- ✅ จุดสีแดง/ส้ม/เหลือง โผล่ในหน้ารายเดือนด้วย
- ✅ ทุกหน้าเดิม (ภาพรวม ปฏิทิน รายเดือน บัตร เงินเก็บ ทำนาย หารครึ่ง) ยังเปิดได้ปกติ
- ✅ ทุกแผงลอยเปิดได้ไม่มี error
